[package]
name = "storage"
version = "0.1.0"
edition.workspace = true

[dependencies]
graph = { path = "../graph" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
uuid = { version = "1", features = ["v4"] }
